import 'package:flutter/material.dart';

class AppColors {
  // Warna Utama (Hijau Lime Khas Iritin)
  static const Color primary = Color(
    0xFFCCFF00,
  ); // Sesuaikan dengan hex desainmu
  static const Color primaryDark = Color(0xFF99CC00);

  // Warna Background & Surface
  static const Color background = Color(0xFFFFFFFF);
  static const Color cardBg = Color(0xFFF8F9FA);

  // Warna Text
  static const Color textPrimary = Color(0xFF1A1A1A); // Hitam pekat
  static const Color textSecondary = Color(0xFF888888); // Abu-abu

  // Warna Indikator (Pemasukan/Pengeluaran)
  static const Color income = Color(0xFF4CAF50); // Hijau uang
  static const Color expense = Color(0xFFE53935); // Merah
}
